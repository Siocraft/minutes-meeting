# Minutes of the meeting AI

To run the AI run

```bash
python -m venv env

source env/bin/activate

pip install --upgrade pip

pip install openai
pip install python-docx
pip install pydub

brew install ffmpeg
pip install tqdm
```
